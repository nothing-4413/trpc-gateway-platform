global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "tgw-gateway"
    metrics_path: "/metrics"
    static_configs:
      - targets:
          - "tgw-gateway:8080"
        labels:
          service: "tgw-gateway"
          env: "local"
server:
  name: "tgw-gateway"
  host: "0.0.0.0"
  port: 8080

runtime:
  io_threads: 2
  worker_threads: 4

log:
  level: "info"
  path: "logs/gateway.log"
  async: false

gateway:
  enable_auth: true
  enable_rate_limit: true
  enable_tracing: true
  request_timeout_ms: 3000